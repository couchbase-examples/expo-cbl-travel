export const __esModule: boolean;
/**
 * FragmentTests - reminder all test cases must start with 'test' in the name of the method or they will not run
 * */
export class FragmentTests extends _testCase.TestCase {
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testBasicGetFragmentValues(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testBasicSetFragmentValues(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testGetDocFragmentWithID(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testGetDocFragmentWithNonExistingID(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testGetFragmentFromDictionaryValue(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testGetFragmentFromArrayValue(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testGetFragmentFromInteger(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testGetFragmentFromFloat(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testGetFragmentFromDouble(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testGetFragmentFromBoolean(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testGetFragmentFromDate(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testGetFragmentFromString(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testGetNestedDictionaryFragment(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testGetNestedNonExistingDictionaryFragment(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testGetNestedArrayFragments(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testGetNestedNonExistingArrayFragments(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testDictionaryFragmentSet(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testDictionaryFragmentsetData(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testDictionaryFragmentSetArray(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testNonDictionaryFragmentsetValue(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testArrayFragmentsetData(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testArrayFragmentSetArrayObject(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testArrayFragmentSetArray(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testNonExistingArrayFragmentsetValue(): Promise<ITestResult>;
    /**
     * @returns {Promise<ITestResult>} A promise that resolves to an ITestResult object which contains the result of the verification.
     */
    testOutOfRangeArrayFragmentsetValue(): Promise<ITestResult>;
}
import _testCase = require("./test-case.cjs");
//# sourceMappingURL=fragment-test.d.cts.map