import {NoResults} from "@/components/noResults/NoResults";

export function NoLandmark() {
    return (
        <NoResults message="🛑 No Landmarks Found - use the filtering and search to change your search results."/>
    );
}
